server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    charset utf-8;

    location = /health {
        access_log off;
        default_type text/plain;
        return 200 "healthy\n";
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
